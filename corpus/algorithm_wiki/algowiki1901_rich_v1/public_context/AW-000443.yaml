statement: 'Chandran and Hochbaum is included as a named whole-algorithm context card for a classical graph task. The task is to analyze the classical input-output problem, not to certify an end-to-end improvement claim. Input semantics: An explicit graph instance with vertices, edges, weights, labels, or local adjacency as required by the named task. Output semantics: Pattern occurrence positions, matches, or the associated string-search report. Classical algorithm summary: Chandran and Hochbaum is a graph algorithm record. Classical complexity metadata: time O(\min(Vk, E)+\sqrt{k}\min(k^2, E)); space O(E). The stated classical model is Word RAM. Public metadata/source text adds: Chandran and Hochbaum graph graph V: number of vertices, E: number of edges, (k: size of maximum matching) O(\min(Vk, E)+\sqrt{k}\min(k^2, E)) Given an explicit graph instance, the concrete classical graph task associated with Chandran and Hochbaum. Classical complexity recorded for this row: time O(\min(Vk, E)+\sqrt{k}\min(k^2,
  E)); space O(E); computation model Word RAM. Dominant bottleneck: graph traversal, priority/frontier maintenance, and materializing graph outputs. The useful review questions are whether the named structure exposes a smaller decision, witness, estimation, or algebraic subproblem; whether the full output size dominates any asymptotic gain; and whether the stated access model is realistic for the original input. Provenance used for reconstruction includes public metadata or abstract sources such as Practical and theoretical improvements for bipartite matching using the pseudoflow algorithm; [1105.1569] Practical and theoretical improvements for bipartite matching using the pseudoflow algorithm. Source-backed uncertainty or assumptions: The AlgorithmWiki row marks the classical method as randomized.'
input_model: explicit_graph_problem
access_model: adjacency_list_query
output_contract: full_solution
promises:
- source_backed_classical_context
- whole_algorithm_analysis_input
- classical_randomized_algorithm
size_parameters:
- 'Parameter definition: V: number of vertices, E: number of edges, (k: size of maximum matching)'
ambiguities:
- The AlgorithmWiki row marks the classical method as randomized.
- The card is a discovery input and does not encode benchmark answers or evaluator labels.
