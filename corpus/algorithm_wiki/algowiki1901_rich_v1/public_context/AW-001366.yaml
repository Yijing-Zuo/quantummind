statement: 'Cannon (Classical 2D) is included as a named whole-algorithm context card for a classical image processing task. The task is to analyze the classical input-output problem, not to certify an end-to-end improvement claim. Input semantics: An image, grid, point cloud, or signal representation with local or hierarchical processing parameters. Output semantics: A transformed image, segmentation, quantization, feature set, or detected structure. Classical algorithm summary: Cannon (Classical 2D) is a image processing algorithm record. Classical complexity metadata: time n^3/p + 2sqrt(p) * (t_s + n^2/p t_w); work n^3 + 2sqrt(p) * (p*t_s + n^2*t_w); span/depth n^3/p + 2sqrt(p) * (t_s + n^2/p t_w); processors p. The stated classical model is 2D mesh. Public metadata/source text adds: Cannon (Classical 2D) image_processing image_processing nxn: size of matrices, t_s: message startup time, t_w: per-word comm time n^3/p + 2sqrt(p) * (t_s + n^2/p t_w) Given an explicit image or grid of pixel
  values, compute the transformed image, segmentation, or detected structure the concrete classical image processing task associated with Cannon (Classical 2D). Classical complexity recorded for this row: time n^3/p + 2sqrt(p) * (t_s + n^2/p t_w); space unknown; computation model 2D mesh. Dominant bottleneck: dominant bottleneck remains uncertain from the available public source metadata. The useful review questions are whether the named structure exposes a smaller decision, witness, estimation, or algebraic subproblem; whether the full output size dominates any asymptotic gain; and whether the stated access model is realistic for the original input. Provenance is limited to AlgorithmWiki metadata and source-link facts; unresolved details are kept as ambiguities. Source-backed uncertainty or assumptions: No independent fetched public metadata source resolved this row beyond AlgorithmWiki metadata/source-link facts.'
input_model: explicit_image_processing_problem
access_model: explicit_input_instance
output_contract: full_solution
promises:
- source_backed_classical_context
- whole_algorithm_analysis_input
size_parameters:
- 'Parameter definition: nxn: size of matrices, t_s: message startup time, t_w: per-word comm time'
- p = stated number of processors (p)
ambiguities:
- No independent fetched public metadata source resolved this row beyond AlgorithmWiki metadata/source-link facts.
- Some details are reconstructed from AlgorithmWiki metadata rather than an independently fetched full public description.
- The card is a discovery input and does not encode benchmark answers or evaluator labels.
