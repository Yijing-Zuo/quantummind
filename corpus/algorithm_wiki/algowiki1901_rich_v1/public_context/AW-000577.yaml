statement: 'X Chen is included as a named whole-algorithm context card for a classical graph task. The task is to analyze the classical input-output problem, not to certify an end-to-end improvement claim. Input semantics: An explicit graph instance with vertices, edges, weights, labels, or local adjacency as required by the named task. Output semantics: An edit-distance value, alignment, or dynamic-programming table information for the input strings. Classical algorithm summary: X Chen is a graph algorithm record. Classical complexity metadata: time O(V!); space O(wV^2). The stated classical model is Word RAM. Public metadata/source text adds: X Chen graph graph V: number of vertices in the larger of the two graphs, S: size of the search tree (\exp(V)), w: the beam width parameter for how many of the best nodes are kept at each layer of the beam search (O(V)) O(V!) Given an explicit graph instance, the concrete classical graph task associated with X Chen. Classical complexity recorded for
  this row: time O(V!); space O(wV^2); computation model Word RAM. Dominant bottleneck: graph traversal, priority/frontier maintenance, and materializing graph outputs. The useful review questions are whether the named structure exposes a smaller decision, witness, estimation, or algebraic subproblem; whether the full output size dominates any asymptotic gain; and whether the stated access model is realistic for the original input. Provenance used for reconstruction includes public metadata or abstract sources such as An efficient algorithm for graph edit distance computation; Redirecting.'
input_model: explicit_graph_problem
access_model: adjacency_list_query
output_contract: full_solution
promises:
- source_backed_classical_context
- whole_algorithm_analysis_input
size_parameters:
- 'Parameter definition: V: number of vertices in the larger of the two graphs, S: size of the search tree (\exp(V)), w: the beam width parameter for how many of the best nodes are kept at each layer of the beam search (O(V))'
ambiguities:
- The card is a discovery input and does not encode benchmark answers or evaluator labels.
