statement: 'Randomized HITS is included as a named whole-algorithm context card for a classical randomized sampling task. The task is to analyze the classical input-output problem, not to certify an end-to-end improvement claim. Input semantics: A probability model, data set, or sampler description together with requested estimator parameters. Output semantics: A sample, estimate, or approximate value with the stated randomized or statistical semantics. Classical algorithm summary: Randomized HITS is a randomized sampling algorithm record. Classical complexity metadata: time O(mn \log n); space O(n). The stated classical model is Word RAM. Public metadata/source text adds: Randomized HITS randomized_sampling randomized_sampling n: number of pages, m: number of hyperlinks O(mn \log n) Given explicit input data and classical randomness as part of the algorithmic model, produce the sampled or estimated output described by the record. Classical complexity recorded for this row: time O(mn \log
  n); space O(n); computation model Word RAM. Dominant bottleneck: precision, convergence, sample complexity, and estimator variance. The useful review questions are whether the named structure exposes a smaller decision, witness, estimation, or algebraic subproblem; whether the full output size dominates any asymptotic gain; and whether the stated access model is realistic for the original input. Provenance used for reconstruction includes public metadata or abstract sources such as Stable algorithms for link analysis.'
input_model: explicit_numerical_problem
access_model: explicit_numeric_parameters
output_contract: estimate
promises:
- source_backed_classical_context
- whole_algorithm_analysis_input
size_parameters:
- 'Parameter definition: n: number of pages, m: number of hyperlinks'
ambiguities:
- The card is a discovery input and does not encode benchmark answers or evaluator labels.
