statement: 'Round-robin scheduling is included as a named whole-algorithm context card for a classical optimization task. The task is to analyze the classical input-output problem, not to certify an end-to-end improvement claim. Input semantics: An explicit feasible-region, objective, scheduling, assignment, or knapsack-style instance. Output semantics: An optimal or approximate feasible solution with its objective value or schedule/assignment representation. Classical algorithm summary: Round-robin scheduling is a optimization algorithm record. Classical complexity metadata: time O(n); space O(n+k). The stated classical model is Word RAM. Public metadata/source text adds: Round-robin scheduling optimization optimization n: number of tasks (intervals), k: number of machines (resources) O(n) Given an explicit optimization instance, compute a feasible solution, schedule, assignment, or approximation with the stated classical guarantees. Classical complexity recorded for this row: time O(n);
  space O(n+k); computation model Word RAM. Dominant bottleneck: dominant bottleneck remains uncertain from the available public source metadata. The useful review questions are whether the named structure exposes a smaller decision, witness, estimation, or algebraic subproblem; whether the full output size dominates any asymptotic gain; and whether the stated access model is realistic for the original input. Provenance is limited to AlgorithmWiki metadata and source-link facts; unresolved details are kept as ambiguities. Source-backed uncertainty or assumptions: No independent fetched public metadata source resolved this row beyond AlgorithmWiki metadata/source-link facts.'
input_model: explicit_optimization_problem
access_model: explicit_input_instance
output_contract: assignment_or_schedule
promises:
- source_backed_classical_context
- whole_algorithm_analysis_input
size_parameters:
- 'Parameter definition: n: number of tasks (intervals), k: number of machines (resources)'
ambiguities:
- No independent fetched public metadata source resolved this row beyond AlgorithmWiki metadata/source-link facts.
- Some details are reconstructed from AlgorithmWiki metadata rather than an independently fetched full public description.
- The card is a discovery input and does not encode benchmark answers or evaluator labels.
