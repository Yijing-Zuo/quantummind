statement: 'De period-structure probe. This is a subroutine/query-model probe, not an end-to-end claim for the original algorithm. The probe isolates an actual period, cycle length, recurrence, or modular structure-finding subroutine. Original input context: One or more explicit matrices or linear systems over the stated arithmetic domain. Original output context: The matrix product or bilinear multiplication output. Original bottleneck noted for the rich context card: arithmetic over dense or structured matrices and the cost of producing matrix-sized outputs. The periodic promise is introduced only when the public source text supports cycle or period language. Coherent oracle construction, promise checking, and data-mapping cost remain expert obligations. The original algorithm may still have full-output, preprocessing, or data-loading barriers.'
input_model: explicit_combinatorial_problem
access_model: coherent_boolean_oracle
output_contract: exact_value
promises:
- periodic_structure
- oracle_model_assumption
size_parameters:
- 'Parameter definition: n: length of one of the integers, in bits'
ambiguities:
- This probe is assumption-bearing and is not an end-to-end speedup claim.
- periodicity may be incidental metadata rather than the task bottleneck
- The parent context card and source records should be checked before live use.
